// Placeholder — les collections (destinations, bases, pois, dishes, gems, budget,
// pratique, images) et leurs schémas zod arrivent au ticket 20 (FR-2, ARCHITECTURE.md §3).
// En Astro 6 ce fichier vit à la racine de src/ (src/content/config.ts n'existe plus).
export const collections = {};
